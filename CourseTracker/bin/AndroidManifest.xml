<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ids.application.main"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.ids.application.main.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentProfile" 
            android:label="@string/app_name"></activity>
        <activity android:name=".SampleCalendarView" 
            android:label="@string/app_name"></activity>
        <activity android:name=".InstructorTA" 
            android:label="@string/app_name"></activity>
        <activity android:name=".AboutUs" 
            android:label="@string/app_name"></activity>
        <activity android:name=".Instructors" 
            android:label="@string/app_name"></activity>
        <activity android:name=".TAs" 
            android:label="@string/app_name"></activity>
        <activity android:name=".AddEditInstructorTA" 
            android:label="@string/app_name"></activity>
        <activity android:name=".AddEditClassStudySessions"
            android:label="@string/app_name"></activity>
        </application>

</manifest>
